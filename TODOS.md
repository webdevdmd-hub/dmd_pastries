# TODOS

Deferred work with a reason. Items land here when they are real but outside the current plan's blast radius.

## Deferred from the UI/UX rebuild plan (2026-08-17)

| #   | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Why deferred                                                                                                                                                                           | Effort (human → CC) | Priority                          | Blocked by |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | --------------------------------- | ---------- |
| T-A | **Dark mode.** MIGRATION.md Phase 8 in full: `.dark` block with the measured v3 values, `themes.ts` → light/dark/system, `pastries-pos-theme` localStorage migration with a forward-compatible reader, pre-paint script, `palette(mode)`.                                                                                                                                                                                                                                                                                                                                                                                               | Cut from the DoD by owner decision. Nobody at a bakery counter needs it and it doubles QA across 147 routes. Token layer is already dark-ready, so this is an addition, not a rewrite. | L → M               | P2 when the sellable phase starts | B3         |
| T-B | **Frozen seed dataset + staging tenant** for the visual harness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | >1 day, new infrastructure. Without it `visual:diff` reports noise on live data and gets abandoned — `capture.mjs` predicts this in its own header.                                    | L → M               | P2                                | —          |
| T-C | Expand `scripts/visual/routes.mjs` from 34 → 147 routes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Moves together with T-B. 147 noisy baselines is worse than 34 noisy baselines, so do not do this one alone.                                                                            | M → S               | P3                                | T-B        |
| T-D | GitHub Actions CI running `pnpm verify`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | New infra; interacts with the Dokploy auto-deploy path. The pre-push hook covers most of it and is bypassable with `--no-verify`.                                                      | S → S               | P2                                | —          |
| T-E | Rewrite `scripts/check-no-any.mjs` onto one shared debt-counting chassis.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Two bespoke debt scripts is the DRY violation that surfaces in six months. Outside the rebuild's blast radius.                                                                         | M → S               | P3                                | A0.2       |
| T-F | `pnpm design:status` — one command printing utilities remaining, per-directory breakdown, which rules are at `error`, which routes have baselines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Every number already exists somewhere; nothing aggregates them. Makes the migration a countdown instead of a vibe.                                                                     | S → S               | P2                                | A0.2       |
| T-G | Mobile breakpoints for the 146 non-POS routes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Genuinely adjacent scope. An owner checking yesterday's sales on a phone is a real use case that no track covers.                                                                      | XL → L              | P3                                | Track C    |
| T-H | ~~Screen-reader semantics for the empty / filtered / failed trio.~~ **Closed by E1.** The three states in `components/shared/collection-state.tsx` announce differently by construction: empty is static content with a heading, filtered is `role="status"` + `aria-live="polite"` because it appears and disappears as someone types, and failed is `role="alert"` because it interrupts. Distinct wording too, not just distinct colour.                                                                                                                                                                                             | Was: three visually distinct states that announce identically.                                                                                                                         | S → S               | done 2026-08-17                   | E1         |
| T-I | Chart empty state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A chart with no data is a distinct, common, ugly state and DESIGN.md does not cover it.                                                                                                | S → S               | P2                                | B5         |
| T-J | Keyboard navigation and focus-order pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | C0.1 changes the focus ring across all 584 components with no verification step attached.                                                                                              | M → S               | P2                                | C0.1       |
| T-K | Runbooks for "visual diff is red everywhere" (→ dataset drift, not CSS), "a page lost its background after A1" (→ check the alias layer for a missing utility name), "session expired mid-capture".                                                                                                                                                                                                                                                                                                                                                                                                                                     | R4's Tailwind-watcher runbook shows the right standard; these three peers lack it.                                                                                                     | S → S               | P3                                | —          |
| T-L | POS tile: out-of-stock badge and Variants button can collide horizontally on a narrow tile — both are `absolute top-3`, opposite corners, on a 146px minimum tile. Pre-existing, not introduced by the A0.1 tap-target fix.                                                                                                                                                                                                                                                                                                                                                                                                             | Needs a card layout decision, not a one-line fix.                                                                                                                                      | S → S               | P2                                | C1         |
| T-M | Command-palette navigation for the Ledger register.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Proposed during the v3 design consultation and deferred there (DESIGN.md §10). Navigation architecture, not design system.                                                             | L → M               | P3                                | —          |
| T-N | RTL / Arabic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Confirmed out of scope by the owner. Separate workstream: bidirectional layout across 147 routes, Arabic type, RTL tables, Arabic tax invoices.                                        | XL → L              | P3                                | —          |
| T-O | Thermal receipt print stylesheet (58/80mm, no colour, no grey).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Own constraints, own spec.                                                                                                                                                             | M → S               | P3                                | —          |
| T-P | **Silent `?limit=100` truncation in three pickers.** `getManufacturingProducts`, the purchasing ingredient options and the manufacturing inventory options each request `?limit=100` and receive a bare array with no `total`. A tenant with more than 100 rows sees 100, with no error and no notice, and the frontend cannot detect it. Fix properly by returning a total and rendering "Showing 100 of 150 — refine your search"; the presentation-only alternative is a cry-wolf heuristic on `rows.length === limit`.                                                                                                              | Found while building E5. The honest fix is a backend contract change, so it sits outside E5's presentation-only boundary and needs its own decision.                                   | S → S               | P2                                | —          |
| T-Q | **Capture `/login` in a separate unauthenticated pass.** It redirects to the role landing page whenever a session exists, and the visual harness needs a hand-established authenticated session to reach the other 34 routes, so within one run the two requirements are mutually exclusive. Removed from `routes.mjs` and its bogus baseline deleted: the committed `login` baseline was a screenshot of `/dashboard/admin` — a real page, distinct from its neighbours, passing every other guard, and the wrong route. Needs either a second capture profile with cookies cleared, or a `--no-auth` mode that starts a clean daemon. | Found when D1 changed the login surface and the "before" turned out never to have been login. The redirect assertion now prevents a recurrence; this is the missing capability.        | S → S               | P2                                | —          |
| T-R | **PO list response carries no receiving data.** `service.go:3769` builds every list row with `includeItems=false` and no document chain, so `order.items` is always empty on `/purchasing/orders`. Two display fixes are blocked on it: the list cannot show ordered-versus-received magnitude (a PO 1-of-500 received and one 499-of-500 both read "Partially received"), and the Next Step cell cannot tell a billable order from one already billed, so it says "Ready to bill" either way. Fix by adding `stock_line_count`, `received_line_count`, `unreceived_value` and `has_active_bill` to `PurchaseOrderResponse` on the list path. | Found during the PO UI/UX audit. The fix is a backend contract change, so it sits outside the audit's UI-only boundary. `purchase-order-quantities.ts` already computes all four client-side and is ready to consume them. | S → S | done 2026-09-16 (ISSUE-033, fix(qa) on branch claude/qa-dmdpastries-app-e9bc0b): all four fields shipped; Next Step reads "Billed" and shows line progress | — |
| T-S | **Visual-harness tab entries defeat the wrong-page guard.** The five `/inventory?view=...` rows in `scripts/visual/routes.mjs` all legitimately resolve to pathname `/inventory`, and `capture.mjs` compares pathname only — so if `?view=` broke and every tab rendered "All items", all five baselines would still pass the guard built to catch exactly that. Needs a content assertion (active-tab marker) or query-aware comparison. | Found by the ship review army. Harness work, same family as T-B/T-C. | S → S | P3 | — |
| T-T | **Movements "Created by" filter needs a user picker.** The control collected free text while the backend compares `created_by_user_id` (a UUID), so it could never match; the ship review removed the dead control. Reinstate it as a user picker bound to the real param. | A filter that can never match is worse than none; the picker needs a users lookup this toolbar doesn't have. | S → S | P3 | — |
| T-U | **Product-type filtering on Stock Transfers and Location Balances needs backend fields.** Both panels shipped a product-type select the backend never read (`StockTransferListQuery` / `LocationBalanceQuery` have no ProductType); the ship review removed the lying controls. Add the fields + EXISTS predicates (mirror `applyInventoryFilters`) and restore the selects. Also: `product_type` is accepted unvalidated on the list/movement paths — an invalid value returns an empty list instead of a 400 (`item_type` gets one). | Backend contract change; the movements/list filters landed tested, these two panels should get the same treatment rather than a rushed copy. | S → S | P3 | — |
| T-V | ~~**No way to close a workspace.**~~ **Half done (a348dd0).** Shipped: `businesses.status` is now enforced -- both auth paths refuse a non-active business, with platform admins short-circuiting first so closing cannot lock out the people who reopen it -- and an owner can close their own workspace from Settings behind a typed confirmation, audited before the commit. Status was removed from `UpdateBusinessRequest`, because that route needs only settings-manage and a settings manager should not be able to close the business while editing its name. **Still open:** erasure. Removing a tenant's data means an ordered teardown across the 76 tables carrying a `business_id`, none of which cascade, and it needs a decision about which accounting records a tenant may erase and what must survive. Filed separately as T-W. | The enforcement gap and the owner-facing close were the reversible half and shipped together. Erasure is a retention policy call, not an implementation detail. | L → M | done 2026-09-08 (close) / see T-W (erase) | — |
| T-W | **Erase a closed workspace.** Hard-delete a business and everything under it. The pattern already exists for users: `hardDeletePreview` separates blocking counts from cleanup counts, and `hardDeleteUser` runs the guarded teardown with a mandatory reason and an audit entry -- mirror both for businesses. The work is the teardown itself: 76 tables carry a `business_id` and every `REFERENCES businesses(id)` is declared without `ON DELETE CASCADE`, so it needs an explicit child-first order or deferred constraints in one transaction. | Blocked on a decision, not on effort: which accounting records may a tenant erase, and what must survive erasure. Answer that before anyone estimates the teardown. | L → M | P3 | T-V (done) |
| T-X | **Branded 404 page.** `frontend/src/app` has no `not-found.tsx`, so an unknown route (a mistyped URL, a stale bookmark after a route rename) renders the stock Next.js "404 This page could not be found." with no wordmark, no link back to the app, and none of the ErrorState styling `app/error.tsx` already uses for crashes. Add `not-found.tsx` on the same ErrorState + "Back to dashboard" pattern. | Found by /qa on app.dmdpastries.com, 2026-09-14 (ISSUE-002, low). Cosmetic, no lost task: the browser back button works. Deferred by tier, not by difficulty. | S → S | done 2026-09-14 (fix(qa) 8f3996b, branch claude/qa-dmdpastries-app-e9bc0b) | — |
| T-Y | **Landing header overflows at 375px.** Measured in-page at a 375px viewport: the "Create account" pill spans x=219 to 379, four pixels past the viewport edge and clipped by `overflow-x-hidden`, and the wordmark link ("Pastries POS" + "Bakery operations", 135px wide) wraps onto four text lines. Fix in `app/page.tsx`: hide the "Bakery operations" tagline below `sm:`, and shrink the pill padding or drop its arrow below `sm:`. | Found by /qa on app.dmdpastries.com, 2026-09-14 (ISSUE-003, low). Both buttons stay tappable; nothing is lost. Same family as T-G. | S → S | done 2026-09-14 (fix(qa) fe437c7, branch claude/qa-dmdpastries-app-e9bc0b) | — |
| T-Z | **Signup "Business name" has no autocomplete token.** Every other signup input declares one (`name`, `email`, `tel`, `new-password`); the business name declares none, so browser and password-manager autofill skip it. One attribute: `autoComplete="organization"` in `components/auth/signup-form.tsx`. | Found by /qa on app.dmdpastries.com, 2026-09-14 (ISSUE-004, low). WCAG 1.3.5 friction only. | S → S | done 2026-09-14 (fix(qa) 0d694fb, branch claude/qa-dmdpastries-app-e9bc0b) | — |
| T-AA | **Bakery order status changes have no confirmation.** On `/orders/[id]` the status strip (confirmed, in production, ready, delivered, completed, cancelled) fires the PATCH on a single click; cancelling ORD-000001 during QA was one click, no dialog, no toast, no undo. At minimum `cancelled` needs a typed or two-step confirmation; decide which other transitions do. | Found by /qa on app.dmdpastries.com, 2026-09-14 (ISSUE-007, medium). Deferred because which transitions deserve a confirm is a product call, not a code detail; the fix itself is small once decided. | S → S | done 2026-09-14, VERIFIED LIVE (fix de75ec3): on ORD-000002 the dialog fired with zero writes, Keep order backed out with zero writes, and only Cancel order sent the PATCH | — |
| T-AB | **Unnamed controls and unassociated error text in the register and statements.** The `Show prices` checkbox on `/pos` (16x16, no accessible name, below the 48px counter minimum), the two checkboxes on the trial balance filters, and the three inputs in the POS checkout dialog (discount, paid amount, cash handed over) have no accessible name; form error messages (customer, opening stock) render as plain spans with no `aria-invalid` or `aria-describedby` on the input. | Found by /qa, 2026-09-14 (ISSUE-008, low). Same family as ISSUE-001 (fixed dba4d68) but each needs its own label wiring. | S → S | done 2026-09-14 (fix(qa) 9d3e36b, branch claude/qa-dmdpastries-app-e9bc0b) | — |
| T-AC | **DESIGN.md weight and size violations in-app.** Weight 700 via `font-bold` on the trial balance / P&L / balance sheet headings, `Account`/`Debit`/`Credit` headers and section rows; KPI numbers on the cashier and production dashboards and `/ingredients`; `All Journals`, `Manufacturing`. The POS `CMD + K` hint renders at 9.92px / 700. Money cells without `tabular-nums` on `/products` (2) and `/settings/tax-rates` (1). | Found by /qa, 2026-09-14 (ISSUE-009, low). Class-driven, so the CSS reset cannot fix it; belongs with the lint rule from the css-element-reset learning. | S → S | done 2026-09-14 (fix(qa) b467f80, branch claude/qa-dmdpastries-app-e9bc0b) | A0.2 |
| T-AD | **Copy that contradicts itself across screens.** `/payments` shows `Type: Not Set` for a completed cash POS payment; `/profile` says `Default tax 0%` while `/settings/tax-rates` marks `UAE VAT 5%` as Default; `/onboarding` says `100% complete` while the dashboard checklist said `2 of 3 steps done` (the checklist step was `Make your first sale`; onboarding tracks setup only). Pick one source of truth per fact. | Found by /qa, 2026-09-14 (ISSUE-010, low). | S → S | done 2026-09-14 (fix(qa) 96f4432, branch claude/qa-dmdpastries-app-e9bc0b) | — |
| T-AE | **Small register frictions.** The POS cart is lost on a full page reload (no persistence); creating a bakery order shows no success toast while creating a customer does; after a refused checkout the client polls `/pos/checkout-status/{id}` and gets a 404 `checkout not found` in the console. | Found by /qa, 2026-09-14 (ISSUE-011, low). | S → S | done 2026-09-14 (fix(qa) b99bfa5, branch claude/qa-dmdpastries-app-e9bc0b) | — |
| T-AF | **Pre-existing red test.** `TestGenericBusinessUpdateCannotChangeStatus` in `backend/internal/modules/businesses/close_workspace_test.go` fails on `main`: `UpdateBusinessRequest` in `dto.go` still carries `Status`. Either remove the field (the test's intent, per T-V) or retire the guard. | Noticed while running the businesses package tests on 2026-09-14; not caused by and not fixed in the QA branch. | S → S | done 2026-09-14: not a product defect. functionSourceIn over-captured on CRLF (its 
}
 fallback cannot match 
}
, and dto.go has no funcs), so the guard matched Status in a later struct. Helper bounded + 4 new tests; UpdateBusinessRequest never carried Status | T-V |
| T-AG | **ISSUE-006 was fixed in the wrong module; the register still shows no out-of-stock badge.** The counter reads `GET /api/v1/products/pos` (products.Handler.POSProducts -> Service.POSProducts -> Repository.POSProducts, returning that module`s ProductResponse), not `GET /api/v1/pos/products`. Commit 8c9618b patched the latter, so nothing changed on screen: the live payload carries no `available_stock_quantity` key and a stock-tracked product at 0 still sells from the tile until checkout refuses it. Apply the same change to products.Repository.POSProducts (join the product-level inventory_items row), add the two nullable quantities to ProductResponse gated on is_stock_tracked, and repoint product_list_carries_stock_guard_test.go at the endpoint the register actually calls. | Found by live verification after the 2026-09-14 deploy. The original bug is unchanged in production; this is an incomplete fix, not a regression. | S -> S | done 2026-09-14, VERIFIED LIVE (fix 939bb11, guard 7cd75fd, deployed e5d8221): payload carries available_stock_quantity 0 and the zero-stock tile shows the badge with add-to-cart disabled | - |

## Deferred from the RBAC QA run (2026-09-18)

Report: `.gstack/qa-reports/qa-report-app-dmdpastries-com-2026-09-18.md`. Products and POS got one server guard per action in that run (ISSUE-065). The modules below keep the old shape: one write guard that *any* of the module's write permissions opens, so the matrix's per-action boxes are not enforced there.

| # | What | Why deferred | Effort (human → CC) | Priority | Blocked by |
| --- | --- | --- | --- | --- | --- |
| T-R | **Per-action guards for the 12 remaining modules.** Same fix as ISSUE-065 (one `permit()` per route in `cmd/api/main.go` plus a routes test that mounts self-naming guards, as in `internal/modules/products/routes_guard_test.go`) for Customers, Suppliers, Recipes, Bakery Orders, Manufacturing batches, Ingredients, Packaging, Branches, Settings, Master Data, Accounting (accounts vs journal entries) and **Roles** (create/edit/delete/permissions.update). Before each module: list which built-in role loses what. Known so far: Cashier has `orders.create` + `orders.payments.manage` but not `orders.edit`/`orders.status.update`/`orders.delete`, so it edits and cancels bakery orders today only through the lump; Cashier has `customers.create` but not `customers.edit`. Each is "seed the permission" or "the role should not do that". Also mirror the frontend menus the way `useProductPermissions` does. | Owner decision (D4): the Products/POS split removed nothing from built-in roles; here it would, so each module needs a product call. | M → M | **P1** (authorization) | per-module decision |
| T-S | `products.images.manage` is not enforceable per route: images upload inside create/edit, so after ISSUE-065 the box does nothing on its own. Needs a field-level check in `UpdateProduct`/`CreateProduct` (image changed ⇒ require the permission). | No seeded role holds it without `products.edit`; low exposure. | S → S | P3 | — |
| T-T | ISSUE-071: Payments denial copy says "`payments.view` or `pos.view`" in `payments-page-client.tsx`, `refunds-page-client.tsx`, `sale-detail-page-client.tsx`. `pos.view` stopped unlocking Payments in 397695c. | Low, copy only. | S → S | P3 | — |
| T-U | ISSUE-072: `/dashboard/admin`, `/production`, `/purchasing` render chrome plus "Couldn't load the dashboard" for a `dashboard.view`-only role (server 403) instead of an access-denied card. Align each client `canView` with its server check. Belongs with the pending "push 3" general dashboard (a `dashboard.view` user matching no persona). | Pairs with push 3, which is its own design. | S → S | P2 | push 3 |
| T-V | ISSUE-073: header shows "Business workspace" for roles without `settings.view`; `useWorkspaceName` falls back to `user.businessName`, which `/auth/me` does not return. Add `business_name` to `/auth/me`. | Low, cosmetic. | S → S | P3 | — |
| T-W | ISSUE-074: two till actions are both labelled "Create order": the bakery-order button and the cart's checkout button ("Create order AED …"). Rename checkout (e.g. "Charge AED …"). | Copy decision on the counter screen. | S → S | P3 | — |
| T-X | `dashboard.isAdminVisible` grants the admin dashboard to role names "admin"/"owner", against c83afb8 ("role names decide nothing"). | Out of the RBAC run's live-proven scope. | S → S | P2 | — |
| T-Y | `components/dashboard/dashboard-page-shell.tsx` has no callers (it also loads the activity feed ungated). Delete it. | Dead code. | S → S | P3 | — |
| T-Z | `pos-discount-control.tsx` label is uppercase with wide tracking at 0.62rem, against DESIGN.md (no uppercase in-app, nothing below 12px); ESLint `design/*` warns. | Pre-existing, visual. | S → S | P3 | — |

## Deferred from the delete audit (2026-09-18)

Report: `.gstack/qa-reports/delete-audit-2026-09-18.md`. Issues 075-097 were fixed in one pass. These items are left over.

| # | What | Why deferred | Effort (human → CC) | Priority | Blocked by |
| --- | --- | --- | --- | --- | --- |
| T-AA | **Storage files are never deleted.** No delete path removes the uploaded object: product, variant, category, ingredient and packaging images, expense receipts and the company logo. Replacing an image orphans the old one. `CopyCategories` copies file references across branches, so cleanup needs reference counting. | Owner: own spec (D2). | M → M | P2 | spec |
| T-AB | **Two no-database test helpers do the same job:** `internal/testsupport/dryrundb` and `internal/testsupport/fakesql`, built in parallel. Merge them into one, and move the tests that use the other. | Came out of parallel fix agents; both work. | S → S | P3 | — |
| T-AC | `manufacturing/repository_tx_test.go` (`TestIngredientsReadsThroughCallerTransaction`, `TestPackagingAndOutputReadThroughCallerTransaction`) fails on its own setup insert (FK to a batch it never created). Broken on main before this work; skips without a database. | Pre-existing test bug. | S → S | P2 | — |
| T-AD | **Product code/SKU/barcode uniqueness may be business-wide in the live DB.** The 000010 indexes are never dropped; the app checks per branch, so a cross-branch duplicate would 500 at insert. Check the live indexes, then align (the bakery-orders product-code generator still numbers per branch). | Needs a live-DB read. | S → S | P2 | — |
| T-AE | Ingredient/packaging category names are unique per business (index 000005), so two branches cannot both have "Dairy". Now a friendly 409 (ISSUE-089); changing to per-branch needs a migration. | Product decision. | S → S | P3 | owner |
| T-AF | Editing an ingredient or packaging item whose unit or category is inactive still returns 404 on save. Lookups still return inactive order and payment statuses. | Adjacent to ISSUE-089, out of its scope. | S → S | P3 | — |
| T-AG | Supplier delete ignores expenses that name the supplier, and editing such an expense later fails with 404. Deactivating a supplier also blocks ingredient and packaging saves that still reference it. | Adjacent to ISSUE-088. | S → S | P3 | — |
| T-AH | `pos.void` exists in the permission matrix but no screen offers Void; voids are API-only. | Feature, not a delete bug. | S → S | P3 | — |
