node_modules
.next
out
dist
coverage
.turbo
.env*
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.tsbuildinfo

# Visual regression captures (large, machine-specific, regenerated on demand).
# Leading slash anchors this to frontend/visual/ only. Unanchored `visual/`
# would also swallow scripts/visual/, which is the harness source.
/visual/
