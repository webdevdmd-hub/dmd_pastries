node_modules
.pnpm-store
.next
out
dist
coverage
pnpm-lock.yaml

# Generated by Next on every dev start and every build, and it carries
# "This file should not be edited". Its contents also FLIP between the two:
# dev writes ./.next/dev/types/routes.d.ts, build writes ./.next/types/routes.d.ts.
# Format-checking a file we must not format made `pnpm verify` fail after any
# `pnpm build` — which is exactly when people run verify.
next-env.d.ts
